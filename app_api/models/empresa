var mongoose = require( 'mongoose' );

var empresaSchema = new mongoose.Schema({
    ruc: String,
    razonSocial: String,
    representante: String,
    direccion: String,
    telefono: String,
    mail: String,
    usuarios: [usuario]
});
